<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Disabled -->
    <item android:state_enabled="false">
        <shape>
            <solid android:color="@color/rs_transparent"/>
            <corners android:radius="12dp"/>
        </shape>
    </item>

    <!-- Pressed (dark üstünde açık bir overlay) -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="@color/rs_alpha_white_10"/>
            <corners android:radius="12dp"/>
        </shape>
    </item>

    <!-- Default -->
    <item>
        <shape>
            <solid android:color="@color/rs_transparent"/>
            <corners android:radius="12dp"/>
        </shape>
    </item>

</selector>
